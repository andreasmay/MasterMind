package mastermind.domain;

import java.util.Arrays;
import java.util.List;

public enum Colour {
    WHITE,
    BLACK,
    GREY,
    YELLOW,
    GREEN,
    BLUE;
    
    public static List<Colour> newColorList(Colour... colors){
        return Arrays.asList(colors);
    }
}
