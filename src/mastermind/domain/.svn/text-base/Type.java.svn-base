package mastermind.domain;

import java.util.Arrays;
import java.util.List;

public enum Type {
    STICK,
    CYRCLE,
    SQUARE;
    
    public static List<Type> newTypeList(Type...types ){
        return Arrays.asList(types);
    }
}
